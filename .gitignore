# build artifact (regenerated by build_static.py; deployed to gh-pages by CI)
site/

# per-theme SQLite DBs + run reports (rebuilt from feeds; persisted to the `data` branch by CI)
themes/*/data/
**/last_run.json

# python
.venv/
__pycache__/
*.pyc

# local secrets
.env
**/.env

# os
.DS_Store
