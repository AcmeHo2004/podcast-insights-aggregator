# build artifacts (regenerated; deployed to gh-pages by CI)
site/
report/

# pipeline working files + private audio clips (clips are NEVER committed/public — email only)
work/
clips/

# per-theme SQLite DBs + run reports (rebuilt from feeds; persisted to the `data` branch by CI)
themes/*/data/
**/last_run.json

# python
.venv/
__pycache__/
*.pyc

# local secrets
.env
**/.env

# os
.DS_Store
